<android.support.design.widget.AppBarLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/appbar"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:theme="@style/AppTheme.AppBarOverlay">
    <!--android:paddingTop="@dimen/appbar_padding_top"-->
    <!--android:elevation="@android:dimen/toolbar_elevation"-->

    <android.support.v7.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="?attr/colorPrimary"
        app:popupTheme="@style/AppTheme.PopupOverlay"/>

    <!--app:layout_collapseMode="pin"   maybe this will do the collapsing tabs-->
    <!--app:layout_scrollFlags="scroll|enterAlways"-->
    <!--TODO: ^Add that back to toolbar element to hide toolbar when scrolling down and show it again when scrolling up-->

    <android.support.design.widget.TabLayout
        android:id="@+id/tabs"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_collapseMode="parallax"/>

    <!--TODO:app:tabMode="fixed", app:tabGravity="fill", and take out the parallax part -->

</android.support.design.widget.AppBarLayout>